import { categorySliderData } from "./categorySliderData";
import { sliderData } from "./sliderData";

const products = [
  // ürün listesi buraya
];

export { categorySliderData, sliderData };
export default products;
